# Python dependencies for optional sidecar services.
# These are installed automatically by `./scripts/setup.sh` into isolated venvs.
# This file serves as a reference / dependency audit manifest.
#
# Each service uses its own venv — you do NOT need to install all of these
# into a single environment. See SETUP.md for details.

# ── ASR (Speech-to-Text) ──────────────────────────────────────
# venv: .venvs/asr-venv
mlx-audio
fastapi
uvicorn
python-multipart

# ── TTS (Text-to-Speech) ──────────────────────────────────────
# venv: .venvs/tts-venv
# mlx-audio          # shared with ASR
misaki[zh]
# fastapi            # shared with ASR
# uvicorn            # shared with ASR
httpx[socks]
num2words
spacy
phonemizer

# ── LLM Post-processing (Vision) ─────────────────────────────
# venv: .venvs/llm-postprocess-venv
mlx-vlm
# httpx[socks]       # shared with TTS
torchvision
# fastapi            # shared
# uvicorn            # shared
pydantic
