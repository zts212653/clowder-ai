---
# AUTO-SCHEDULE SUNSET — 2026-06-06
# `enabled: false` silences the weekly cron pickup for this domain. No invocation
# message lands in thread_eval_sop; SLA / handoffTargetResolver / threadPolicy
# config is preserved here for future re-enable.
#
# Root cause for sunset: silent-fire — cron fired empty 2 weeks in a row (5/30 + 6/6)
# with 0 verdict, 0 ack. F192 doc §323 self-noted the wiring gap when domain landed
# (5/27): "eval-sop-adapter.ts 只 build packet 对象，不写 disk。... 没有 snapshot
# YAML + attribution YAML → bundle + verdict.md 资产管线。要进 publish-verdict 需先
# 加 file-writer 层（~100-150 行独立工作）". The trace producer side of the pipeline
# (SopTrace from session telemetry) was never wired either.
#
# Re-enable condition (remove `enabled: false` or set `enabled: true`):
#   1. SopTrace producer wired (session telemetry → SopTrace pipeline)
#   2. File-writer layer landed (SopEvalResult[] → snapshot/attribution YAMLs → bundle)
#   3. `eval:sop` added to PUBLISH_VERDICT_INSTRUCTIONS_BY_DOMAIN in
#      packages/api/src/infrastructure/harness-eval/eval-cat-invocation.ts so
#      `cat_cafe_publish_verdict` no longer returns 501.
#
# See: docs/features/F192-socio-technical-harness-eval.md (sunset journal entry)
#      docs/ROADMAP.md (P2: eval:sop wiring completion)
#      thread_eval_sop messages 0001780801515905 (4.7 heartbeat) /
#                              0001780801515935 (4.6 决议 C)
enabled: false

domainId: eval:sop
displayName: SOP Compliance Eval
systemThreadId: thread_eval_sop
evalCat:
  catId: opus-47
  handle: "@opus47"
  model: claude-opus-4-7
frequency: weekly
sourceAdapter: sop-trace-eval
threadPolicy:
  role: working-home
  stateSot: registry
  allowedContent:
    - longitudinal-analysis
    - verdict-discussion
    - handoff-drafts
legacyScheduledTaskIds: []
handoffTargetResolver:
  featureId: F192
  ownerCatId: opus
  threadLookup: feature-thread
sla:
  acknowledgeHours: 48
  reevalWithinHours: 336
