---
doc_kind: architecture
description: "Generated read-only Memory Architecture Closure catalog. Do not edit; regenerate from lane-owned manifests."
description_source: model
description_author: codex-sol
description_generated_by: codex-sol@gpt-5.6-sol
description_generated_at: 2026-09-02T07:00:00Z
description_confirmed_by: codex-sol
description_updated_at: 2026-09-02T07:00:00Z
feature_ids: [F152, F200, F221, F227, F231, F255, F260, F263, F271, F276, F281, F282, F287, F312]
topics: [memory, architecture, closure-gate, recall-frame, generated-catalog]
created: 2026-08-28
status: generated
---

# Memory Architecture Closure — generated read-only universe

> **DO NOT EDIT.** Source declarations live in per-surface `assets/memory-surfaces/*/{owner,closure}.yaml` files.
> Those declarations point back to each canonical truth owner; this file is a rebuildable view and owns no Hub,
> store, payload, approval, correction, or forget authority.

Contract: `MemoryArchitectureClosure.v1`; recall projection: `RecallFrame.v0`.
Universe/declaration set equality: **21 = 21**.

## Surface universe

| Surface | Disposition | Authority | Write/capture | Typed cue predicate | Presentation/drill | Consumer + allowed use | Applied/dismissed receipt | Outcome | Invalidation | Evidence ceiling |
|---|---|---|---|---|---|---|---|---|---|---|
| `cat-owned-seed` | active | implemented (main) — The producing cat, owner scope, and current source-linked seed revision jointly bound private seed authority. | implemented (main) — F255 writes cat-owned seeds without promoting them into user, person, or team truth. | implemented (main) — owned_seed_available.v1 admits at most one current owned seed for the exact owner, producing cat, scheduled Present Loop run, and source revision. | implemented (main) — The prompt carries only a content-free seed coordinate and opaque handle; exact-cat drill reads the current private claim on demand. | implemented (main) — Only the producing cat may treat the drilled seed as a private bounded hypothesis and choose silence or one bounded intent. | implemented (main) — Content-free receipts bind owner, consumer cat, invocation, seed coordinate, and source revision; applied additionally requires an exact same-invocation seed intent. | implemented (main) — The bounded outcome is exact intent application with optional existing F255 visit and echo lineage; it does not claim long-term or causal utility. | implemented (main) — Missing seeds, source revision correction, owner or cat scope mismatch, and dormant or retired status fail closed with bounded invalidation reasons. | main |
| `decision` | active | implemented (main) — Accepted ADRs and direct operator decision records own decision truth; Cue Plane owns neither. | implemented (main) — Decision owners write versioned ADRs under repository governance; direct operator decisions remain authoritative in their owner carrier until promoted. | implemented (main) — accepted_decision_required is emitted only when the current direct-owner task message names exactly one ADR anchor; ambiguous, history-only, or non-owner references fail closed. | implemented (main) — The Decision resolver presents a bounded pointer and drills a bounded content window from the exact accepted ADR bytes; F209 title/summary remain navigation metadata only. | implemented (main) — The current agent_route consumer may constrain its current response or action by the accepted ADR; superseded or draft documents and unstated conclusions are forbidden. | implemented (main) — The content-free Cue Plane ledger admits Decision coordinates and records presented, drilled, applied, or dismissed against the exact ADR revision; applied or dismissed suppresses replay of that revision. | implemented (main) — The bounded outcome is the current agent_route response or action: applied means the drilled accepted ADR materially constrained it, dismissed means it did not; no conformance score or single-ADR causality is claimed. | implemented (main) — Canonical ADR bytes bind the revision; correction, path removal, non-accepted status, supersession, or lexical/physical path escape makes the old handle unavailable. | main |
| `diary` | exempt | implemented (main) — The F229 diary lane owns persisted first-person entries, timeline order, edits, and deletion. | implemented (main) — The diary loop persists user-visible first-person entries with source context. | exempt (main) — No bounded invocation consumer or typed relevance predicate exists; the owner-visible /starry reader remains pull-only. | implemented (main) — The diary reader presents current entries and stored detail on owner pull. | implemented (main) — Readers may use entries for first-person continuity without promoting them to person facts or owner truth. | exempt (main) — A pull reader without a qualified invocation opportunity does not justify a proactive applied or dismissed receipt. | exempt (main) — No named invocation consumer has a bounded diary recall decision, so no utility outcome or eval is created. | exempt (main) — Canonical edit and deletion remain diary-owned; with no cue projection there is no proactive receipt to invalidate. | main |
| `entity` | active | implemented (main) — F260 owns Entity identity, revisions, aliases, and retirement state. | implemented (UAT) — Entity capture resolves or proposes a source-linked registry record before use. | implemented (main) — subject_seen resolves a typed Entity identity plus revision before eligibility. | implemented (UAT) — Direct Entity Nudge fanout binds presentation and cooldown to one exact consumer request; the prompt retains a narrow drill route to the authoritative revision. | implemented (main) — The active cat may resolve references and continuity within scope; cues cannot mutate the registry. | implemented (UAT) — F287 receipt states bind delivery and application to the Entity source revision. | implemented (live) — F200 evaluates the shared subject_seen cue family with bounded utility conclusions. | implemented (UAT) — Entity correction, revision change, or retirement invalidates stale cue eligibility. | UAT |
| `episode` | exempt | implemented (fixture) — Source-linked episode artifacts own reconstructed evidence without becoming person facts or owner truth. | implemented (fixture) — Episode producers can write provenance-bearing artifacts from task traces. | exempt (fixture) — The episode catalog contains only its template and has neither a production candidate nor a named invocation consumer. | implemented (fixture) — Source-linked fixtures support bounded pull presentation and replay drill. | implemented (fixture) — Consumers may use source-linked reconstruction as bounded precedent, never as unqualified policy, person fact, or owner truth. | exempt (fixture) — No production candidate or named invocation consumer exists, so no proactive application receipt is created. | exempt (fixture) — Fixture replay proves deterministic structure, not production use or utility; no outcome eval is licensed. | exempt (fixture) — A future active episode must bind source correction, deletion, supersession, and drift; no current proactive projection exists to invalidate. | fixture |
| `event` | active | implemented (main) — F227 event records and linked revisions own Event truth. | implemented (main) — F227 captures source-linked events into the event lane. | implemented (main) — recent_event_available selects only the newest high-confidence F227 event in the exact owner and subject thread within a bounded fifteen-minute window. | implemented (main) — Event timeline and source references provide pull presentation and drill. | implemented (main) — Consumers may establish chronology and continuity while preserving source uncertainty. | implemented (main) — The existing Cue Plane records presented, drilled, applied, or dismissed against the exact Event revision; a terminal outcome suppresses that revision. | implemented (main) — The strictly authenticated response in the event subject thread is the bounded consumer: applied means the Event established chronology or continuity used in that response, dismissed means it did not; no utility causality is claimed. | implemented (main) — Resolve and drill fail closed when the Event revision changes, its source message is deleted or loses visibility, owner/thread scope changes, or the fifteen-minute event window expires. | main |
| `feedback` | exempt | implemented (UAT) — F281 owns source-message identity, feedback disposition, and correction links. | implemented (UAT) — Feedback capture persists a source reference and bounded disposition instead of copying payload into memory. | exempt (main) — Feedback is a control and invalidation input, not a recallable memory surface. | implemented (UAT) — Feedback records drill back to the exact source message and disposition. | implemented (main) — Harness and lane owners may use feedback for correction and evaluation, never as user-profile truth. | implemented (UAT) — Disposition and linked repair state record whether feedback was acted on or dismissed. | implemented (UAT) — Correction and repair state provide a bounded control-loop outcome. | implemented (UAT) — Source correction and disposition changes supersede stale feedback projections. | UAT |
| `global-distillation-f152` | sunset | implemented (main) — F152 owns only its nominations and materialized global-distillation artifacts; it does not own project knowledge or generic recall authority. | implemented (live) — The live producer chain marks, nominates, approves, materializes, and indexes distilled artifacts; 36 retained distilled anchors prove the path, not consumer value. | sunset (live) — No typed predicate ever selected distilled output, and the operator-approved sunset ends investment in one. | sunset (main) — Generic search, graph resolve/traversal, and recent-browse delivery now omit distilled anchors; retained files and index rows are forensic producer evidence only. | sunset (live) — No runtime consumer or allowed-use contract was proven; retained artifacts may be inspected only as forensic producer evidence. | sunset (live) — No applied or dismissed receipt exists because runtime consumption is no longer supported for this surface. | sunset (live) — The operator value decision ends consumer investment after zero named consumer and zero production content; this is not a utility verdict. | sunset (main) — No source-revision invalidation pipeline exists; the shared delivery-eligibility guard prevents retained forensic artifacts from reaching generic search, graph, or recent-browse consumers while the surface is retired. | live |
| `lessons-learned` | active | implemented (main) — The versioned lessons-learned document owns accepted lessons. | implemented (main) — LessonPromotionContract.v1 names the reviewed monolith patch as the sole canonical writer and freezes claim-family dedupe across direct edit, F102 candidate, and F152 downstream paths. | implemented (fixture) — The lane-owned worktree Redis sanctuary predicate binds LL-015 to the revisioned pre-storage guard event without creating a central cue engine. | implemented (live) — F209 search and source anchors provide exact lesson drill on pull. | implemented (main) — Cats may use accepted lessons to avoid repeated failures while preserving source scope. | implemented (fixture) — A refs-only RecallFrame v0 binds the exact LL-015 and predicate revisions to a presented and applied guard receipt; F200 consumption remains observation-only. | implemented (fixture) — The replay records zero recurrence inside the same guarded startup and states guard enforcement as a confounder and causal ceiling. | implemented (main) — Git revisions and explicit supersession update the canonical lesson source. | fixture |
| `library-knowledge` | exempt | implemented (main) — F186 catalog and source revisions own ingested library knowledge. | implemented (main) — F186 ingestion records source, collection, and catalog metadata. | exempt (docs-only) — F186 is an explicit-ingestion and pull-search surface; no named task-class consumer qualifies a proactive cue. | implemented (main) — F186 search and source views provide pull presentation and drill. | implemented (main) — Consumers may use provenance-bearing library content subject to collection and source policy; private collections require explicit authorization. | exempt (docs-only) — Explicit pull reads do not need a proactive applied or dismissed receipt without a named invocation consumer. | exempt (docs-only) — No proactive consumer exists, so Phase F creates neither a bounded outcome nor a utility eval. | implemented (main) — Collection policy and source revision can retire or refresh stale catalog entries. | docs-only |
| `method` | exempt | implemented (main) — Published versioned Method Cards under docs/methods would own reusable procedure truth; the generated index and TEMPLATE do not become candidates. | implemented (main) — Method owners may write and review versioned Method Cards using the repository template and knowledge-object governance. | exempt (main) — E0 found no published Method Card and no named task consumer; no task-to-method detector or Cue Plane entry is created for an empty surface. | implemented (main) — Published Method Cards remain available through exact source pull once they exist; TEMPLATE and the generated index are discovery scaffolding, not method content. | implemented (main) — A future consumer may follow only a published current Method Card within its declared trigger, prerequisites, guardrails, and confidence threshold. | exempt (main) — With no candidate and no consumer, there is no honest applied or dismissed event to record; retrieval of TEMPLATE or index metadata must not fabricate one. | exempt (main) — With no named consumer, Method quality has no bounded outcome claim or keep/tune/sunset eval; the surface must repeat E0 when a real Method Card and consumer appear. | implemented (main) — A future Method candidate is revision-bound to canonical document bytes and explicit lifecycle/supersession metadata; repository deletion removes it from pull discovery. | main |
| `person` | active | implemented (main) — F276 owns approved Person records and field revisions. | implemented (UAT) — F276/F282 capture candidates and require proposal disposition before canonical write. | implemented (main) — subject_seen is a typed trigger over resolved Person identity and revision. | implemented (UAT) — The cue exposes a narrow authenticated Person drill instead of embedding private payload. | implemented (main) — The active cat may personalize the current interaction within F276 visibility and privacy policy. | implemented (UAT) — F287 records presented, drilled, applied, or dismissed states against the source revision. | implemented (live) — F200 evaluates subject_seen utility and publishes a bounded keep verdict. | implemented (UAT) — F276 correction and forget invalidate derived Person cues and their prior revisions. | UAT |
| `proactive-candidate` | exempt | implemented (UAT) — F282 owns candidate state and disposition; candidates are not approved memory authority. | implemented (UAT) — The detector writes bounded candidate records and abstention reasons. | exempt (main) — An unapproved candidate cannot be recalled as memory; only approval may hand it to a canonical lane. | implemented (UAT) — Candidates present source refs and bounded context for human or cat disposition. | implemented (main) — Consumers may approve, defer, or abstain; they may not treat candidate payload as canonical truth. | implemented (UAT) — Proposal, defer, and abstention states provide candidate disposition receipts. | implemented (UAT) — Candidate disposition and approved-lane handoff provide a bounded workflow outcome. | implemented (UAT) — Suppression, expiry policy, source correction, and final disposition end candidate eligibility. | UAT |
| `profile` | active | implemented (main) — F231 profile capsule and Approval Hub revisions own profile truth. | implemented (main) — Profile proposals are reviewed before persistence. | implemented (main) — profile_revision_available is emitted only for a current F231 capsule revision without a prior applied or dismissed terminal receipt. | implemented (main) — Profile can be read through its bounded capsule and Approval Hub surfaces. | implemented (main) — Cats may personalize within approved profile scope and may not infer unapproved fields. | implemented (main) — The existing Cue Plane records presented, drilled, applied, or dismissed against the exact capsule revision; a terminal outcome suppresses that revision on later invocations. | implemented (main) — The strictly authenticated owner-facing response is the bounded consumer: applied means approved Profile context changed that response, dismissed means it did not; no single-revision utility causality is claimed. | implemented (main) — Approval, correction, and revision replacement invalidate stale profile projections. | main |
| `project-knowledge` | active | implemented (main) — Versioned project sources own facts; F209 indexes and cites them without taking authority. | implemented (main) — Repository-governed feature docs and source-linked artifacts capture project knowledge in their owning lanes. | implemented (main) — project_source_required selects only the workflow-bound feature ID, or exactly one explicit owner F-ID when no workflow binding exists; history-only, ambiguous, and non-owner references fail closed. | implemented (main) — The Project Knowledge resolver uses F209 only to locate the canonical feature source, then drills a bounded content window from that exact source revision; index title/summary remain navigation metadata only. | implemented (main) — The current feature task consumer may use the drilled canonical feature source only to ground its current response or action; index summaries do not become authority. | implemented (main) — The Cue Plane records presented, drilled, applied, or dismissed against the exact canonical feature-document revision; applied or dismissed suppresses replay of that revision. | implemented (main) — The bounded outcome is the current feature-task response or action: applied means the exact feature source grounded it, dismissed means it did not; no project-wide score or single-source causality is claimed. | implemented (main) — Canonical feature-document bytes bind the revision; correction, deletion, inactive status, supersession, or a lexical/physical path escape makes the old handle unavailable. | main |
| `provider-local-memory` | exempt | implemented (docs-only) — Each provider and account owner retains native authority; the carrier census records support boundaries without copying content. | exempt (docs-only) — Provider-native write paths stay provider-owned; Clowder AI must not write provider homes or create a shared capture adapter. | exempt (docs-only) — No carrier exposes both a Clowder AI-verifiable immutable item revision and a named proactive consumer. | exempt (docs-only) — Provider ambient/session context has no common authorized item drill; Clowder AI does not normalize or copy it. | exempt (docs-only) — Provider-native context remains account-scoped and is not admitted as a Clowder AI source without explicit owner ingestion. | exempt (docs-only) — Execution or session completion cannot attest that a distinct provider-memory revision was applied. | exempt (docs-only) — Without a named consumer or item receipt, Phase F declares no provider-memory outcome or utility eval. | exempt (docs-only) — Provider correction and deletion remain authoritative; Clowder AI keeps no provider-memory content replica or derived cue to invalidate. | docs-only |
| `recall-consumption-ledger` | exempt | implemented (live) — F200/F263 trace records own delivery and consumption observations only. | implemented (live) — The observation plane records recall delivery and application lifecycle state. | exempt (main) — The ledger observes cues; it must never produce or select recalled truth. | implemented (live) — Trace refs drill to delivery, application, and source revision evidence. | implemented (live) — Eval and lifecycle consumers may use ledger facts as proxies with explicit causal limits. | implemented (UAT) — Presented, drilled, applied, and dismissed states are the ledger's native receipt contract. | implemented (live) — F200 can join observations to bounded outcomes while forbidding proxy-as-correctness claims. | implemented (live) — Source revision and lifecycle repair mark stale observations without rewriting historical trace. | UAT |
| `reflection` | exempt | implemented (main) — F271 reflection artifacts retain sourced and revised hypotheses while canonical truth stays in destination lanes. | implemented (main) — F271 emits bounded typed deltas without rewriting canonical source memory. | exempt (main) — The supply ledger and destination handoff are not a consumption predicate, and no reflection-level invocation consumer is named. | implemented (main) — Reflection artifacts and source refs remain inspectable through pull and destination-owned readers. | implemented (main) — A reflection may be used only as a sourced hypothesis or retrospective; approved facts remain destination-owned. | exempt (main) — F271 production and handoff do not establish a reflection-level invocation application contract. | exempt (main) — No named reflection-level consumer has a bounded keep, tune, or sunset decision, so no utility eval is created. | exempt (main) — Source and destination lanes retain correction and disposition authority; no separate proactive reflection projection exists to invalidate. | main |
| `skill` | active | implemented (main) — Each shipped SKILL.md revision owns executable instruction truth. | implemented (main) — writing-skills governs creation, review, and publication of skill packages. | implemented (main) — Skill descriptions and explicit naming provide typed task-to-skill trigger predicates. | implemented (main) — A matched skill is presented by name and drilled by reading its full SKILL.md. | implemented (main) — The active agent may execute instructions only within user authority and repository scope. | implemented (main) — workspace-navigator prepares an invocation-, consumer-, and package-revision-bound handle; only the real Workspace consumer writes applied, while a separate bounded path writes dismissed. | implemented (main) — The pilot outcome is limited to the Workspace consumer's delivery four-state or not_applicable decision and explicitly excludes task-success or single-skill causal claims. | implemented (main) — Package revision changes invalidate prepared handles and reclassify prior receipts as stale; scoped sync replaces stale distributed copies. | main |
| `taste` | active | implemented (main) — F221 owns approved Taste revisions and correction state. | implemented (main) — Taste proposals flow through Approval Hub before canonical persistence. | implemented (main) — F287 binds the explicit ELI5 phrase to approved_taste_invoked with predicate revision and exact Taste authority. | implemented (UAT) — The cue presents an owner-authenticated drill handle and requires drill before response generation. | implemented (main) — The responding cat may use the drilled Taste only to shape the matching visual explanation; the cue cannot confer write authority. | implemented (UAT) — PR #4019 canonical Alpha proved presented→drilled→applied→html_widget for the ELI5 cue. | implemented (live) — F200 publishes a family-level keep verdict; it does not prove single-anchor causality. | implemented (main) — Predicate and source revisions force revalidation after Taste correction or deletion; stale authority fails closed. | UAT |
| `verdict` | exempt | implemented (live) — F192 definitions and published verdict artifacts own keep, tune, or sunset decisions. | implemented (live) — Eval Hub publishes provenance-bearing verdicts from registered definitions and evidence. | exempt (main) — A verdict is a decision output consumed by governed workflow, not invocation-time memory recall. | implemented (live) — Verdicts drill to definition, evidence bundle, attribution, and snapshot refs. | implemented (main) — Lane owners may use verdicts only for the registered keep, tune, or sunset decision. | implemented (live) — Verdict publication and workflow transition provide delivery and application custody. | implemented (live) — The verdict is itself the bounded decision outcome and records its evidence ceiling. | implemented (live) — A newer evidence window or superseding verdict invalidates prior decision applicability. | live |

## Exact missing owner map

| Surface | Field | Break | Owner refs | Next action |
|---|---|---|---|---|
| — | — | — | — | — |

## Non-overlapping next carriers

Each carrier owns exactly one surface and all of that surface's RED fields. Carrier field sets are therefore
pairwise disjoint; closing one carrier cannot silently mutate another lane's authority.

| Carrier | Surface | RED fields | Owner refs |
|---|---|---|---|
| — | — | — | — |

Strict gate: `pnpm gate:memory-architecture-closure`. It fails on any `missing`, any literal
`unknown`, invalid owner refs, stale generated output, or owner/declaration set inequality.

The current RED is intentional architecture truth, not a CI waiver: downstream carriers remove rows by changing
their lane-owned declaration only after stronger evidence exists.
