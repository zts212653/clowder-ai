id: D7
name: 模式声明
stage: per-turn
order: 700
version: 1
enabled: true

# Content resolution
template: d7-mode-declaration.md
resolver: D7ModeResolver

# Dependencies
inputs:
  - mode

# Variable metadata (canonical source for Console editor)
variables:
  - name: CHAIN_INDEX
    description: 串行链中的当前猫序号
    placeholder: ""
  - name: CHAIN_TOTAL
    description: 串行链中的猫总数
    placeholder: ""
# Override constraints
disableable: true

# Classification (Phase 1 3-axis)
safetyTier: readonly
transparencyTier: visible-by-default
governanceTier: human-gated

# operator-facing
userExplanation: "当前的工作模式（如 voice mode、bootcamp 等）"
