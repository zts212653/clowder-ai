id: R2
name: 路由组装（并行）
stage: per-turn
order: 2300
version: 1
enabled: true

# Content resolution
template: r2-route-parallel.md
resolver: R2RouteParallelResolver

# Dependencies
inputs:
  - routeParallelContext

# Override constraints
disableable: false

# Classification (Phase 1 3-axis)
safetyTier: readonly
transparencyTier: opt-in-view
governanceTier: immutable

# CVO-facing
userExplanation: "并行路由的组装逻辑"
