id: D11
name: Skill 触发
stage: per-turn
order: 1100
version: 1
enabled: true

# Content resolution
template: d11-skill-trigger.md
resolver: D11SkillTriggerResolver

# Dependencies
inputs:
  - skillTrigger

# Variable metadata (canonical source for Console editor)
variables:
  - name: SKILL_NAME
    description: 被触发 skill 的名称
    placeholder: ""
# Override constraints
disableable: true

# Classification (Phase 1 3-axis)
safetyTier: readonly
transparencyTier: opt-in-view
governanceTier: human-gated

# CVO-facing
userExplanation: "自动触发的 skill 上下文"
