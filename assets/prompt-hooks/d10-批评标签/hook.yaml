id: D10
name: 批评标签
stage: per-turn
order: 1000
version: 1
enabled: true

# Content resolution
template: d10-critique-tag.md
resolver: D10CritiqueTagResolver

# Dependencies
inputs:
  - critiqueTag

# Override constraints
disableable: true

# Classification (Phase 1 3-axis)
safetyTier: readonly
transparencyTier: opt-in-view
governanceTier: human-gated

# CVO-facing
userExplanation: "用于标记需要批判性审视的内容"
