id: S11
name: 默认行为
stage: session-init
order: 1800
version: 1
enabled: true

# Content resolution
template: s11-defaults.md
resolver: S11DefaultsResolver

# Dependencies
inputs:
  - defaults

# Variable metadata (canonical source for Console editor)
variables:
  - name: PACK_DEFAULTS_BLOCK
    description: pack 默认行为块
    placeholder: ""
# Override constraints
disableable: true

# Classification (Phase 1 3-axis)
safetyTier: readonly
transparencyTier: opt-in-view
governanceTier: human-gated

# operator-facing
userExplanation: "未明确指定时的默认行为设置"
